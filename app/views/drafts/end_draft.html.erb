<style>
  h1 {text-align: center;}
  h3 {text-align: center;}

</style>

<h1> Done! The draft is closed.</h1>

<h3> Draft Summary: </h3>

<div class = "container text-center">
  <div class = "row">
    <div class = "col-lg-4">
      <div class = "jumbotron">
        <h4> <%= @league.teams.first.name %> </h4>
        <% @league.teams.first.players.each do |player|%>
        <p style = "font-size: 18px;"> <%= player.position + " - " + player.name %> </p>
          <% end %>
    </div>
  </div>

    <div class = "col-lg-4">
      <div class = "jumbotron">
        <h4> <%= @league.teams.second.name %> </h4>
        <% @league.teams.second.players.each do |player|%>
        <p style = "font-size: 18px;"> <%= player.position + " - " + player.name %> </p>
          <% end %>
    </div>
    </div>

    <div class = "col-lg-4">
      <div class = "jumbotron">
        <h4> <%= @league.teams.third.name %> </h4>
        <% @league.teams.third.players.each do |player|%>
         <p style = "font-size: 18px;"> <%= player.position + " - " + player.name %> </p>
          <% end %>
      </div>
    </div>
  </div>

  <div class = "row">
    <div class = "col-lg-1">
    </div>
    <div class = "col-lg-4">
      <div class = "jumbotron">
        <h4> <%= @league.teams.fourth.name %> </h4>
        <% @league.teams.fourth.players.each do |player|%>
        <p style = "font-size: 18px;"> <%= player.position + " - " + player.name %></p>
          <% end %>

        </div>
      </div>

      <div class = "col-lg-2">
      </div>

      <div class= "col-lg-4">
        <div class = "jumbotron">
          <h4> <%= @league.teams.fifth.name %> </h4>
          <% @league.teams.fifth.players.each do |player|%>
          <p style = "font-size: 18px;"> <%= player.position + " - " + player.name %> <p>
            <% end %>
        </div>
        </div>
      <div class = "col-lg-1">
      </div>
  </div>
  <div class="row" text-center>
    <%= button_to "Go Back to League Page", league_path(@draft.league), method: :get, class: "btn btn-default btn-lg" %>
  </div>  
</div>
